@model IEnumerable<Ejercicio03.Models.Empleado>

@{
    ViewData["Title"] = "Index";
}


<h3 class="titulo">Empleados</h3>

<p class="creacion">
    <a class="creacion-link" asp-action="Create">Crear registro</a>
</p>
<table class="tabla" id="datatableCheating">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCompleto)
                @* <a asp-action="Index"
                   asp-route-sortOrder="@ViewData["NombreCompleto"]">@Html.DisplayNameFor(model => model.NombreCompleto)</a> *@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Roles)
                @* <a asp-action="Index"
                   asp-route-sortOrder="@ViewData["Roles"]">@Html.DisplayNameFor(model => model.Roles)</a> *@
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody class="tbody">
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCompleto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Roles.Descripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
