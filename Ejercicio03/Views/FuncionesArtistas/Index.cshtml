@model IEnumerable<Ejercicio03.Models.FuncionesArtista>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @* @Html.DisplayNameFor(model => model.Artistas) *@
                <a asp-action="Index"
                   asp-route-sortOrder="@ViewData["Artistas"]">@Html.DisplayNameFor(model => model.Artistas)</a>
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.Funciones) *@
                <a asp-action="Index"
                   asp-route-sortOrder="@ViewData["Funciones"]">@Html.DisplayNameFor(model => model.Funciones)</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Artistas.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Funciones.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
