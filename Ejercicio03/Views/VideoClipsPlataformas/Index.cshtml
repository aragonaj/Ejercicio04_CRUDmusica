@model IEnumerable<Ejercicio03.Models.VideoClipsPlataforma>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-hover table-sm" id="datatableCheating">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Url)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plataformas)
                @* <a asp-action="Index"
                   asp-route-sortOrder="@ViewData["Plataformas"]">@Html.DisplayNameFor(model => model.Plataformas)</a> *@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VideoClips)
                @* <a asp-action="Index"
                   asp-route-sortOrder="@ViewData["VideoClips"]">@Html.DisplayNameFor(model => model.VideoClips)</a> *@
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Url)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plataformas.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VideoClips.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
